# Google Sheets Configuration
# Email de la cuenta de servicio (del archivo JSON descargado, campo "client_email")
GOOGLE_SERVICE_ACCOUNT_EMAIL=tu-service-account@tu-proyecto.iam.gserviceaccount.com

# Clave privada (del archivo JSON descargado, campo "private_key")
# IMPORTANTE: Mantén las comillas dobles y los \n literales
# El código reemplazará automáticamente \n por saltos de línea reales
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nTu clave privada aquí\n-----END PRIVATE KEY-----\n"

# ID de la hoja de cálculo (de la URL: https://docs.google.com/spreadsheets/d/[ID_AQUI]/edit)
# Solo el ID, NO la URL completa
GOOGLE_SHEET_ID=tu-sheet-id-aqui

# Entorno de ejecución (development o production)
NODE_ENV=development
